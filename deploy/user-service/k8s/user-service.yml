apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: multi-service
spec:
  type: NodePort
  selector:
    app: user-service
  ports:
    - port: 3000
      nodePort: 30007